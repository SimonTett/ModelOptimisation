Ie. we use xhdic as the start job for the analysis, but it in turn
had specified xhdib as its exec.  

We need the exec name to match the start job (at present) so what we call
xhdic.exec is xhdii.exec. This executable differs only that it allows
longer names in files. 



