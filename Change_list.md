# Change log
2026-01-22
- Added update_params.py script to update model parameters & generate evaluation_database info.
-  runSubmit, SubmitStudy & Model now have update_params methods which update parameters from on file config.
                    For Model this will likely need work for derived model types.
- Model, SubmitStudy &  runSubmit have copyConfig methods to allow copying of a configuration 
- archive now makes use of copyConfig method which makes it easier to generalise.
- runSubmit.run_dfols now supports an evaluation database (generated by update_params) to allow resuse of model evaluations. 

2026-03-07
- added some env variables setup by runAlgorithm.py to support logging.
- filename keys in log config dict get expanded and expand checks if expansion works. 

2026-03-10
- added ability to have different post-processing for different models in a multi-model configuration.
- model now includes reference attribute and config_name method.
- Cleaned up how logical_info handles models. Main change is that multi-model function should return  obs AND list of models
